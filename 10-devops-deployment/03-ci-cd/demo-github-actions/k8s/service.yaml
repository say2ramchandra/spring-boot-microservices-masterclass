apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: product
  labels:
    app: product-service
spec:
  type: LoadBalancer
  selector:
    app: product-service
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
