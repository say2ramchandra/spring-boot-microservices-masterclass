# Test profile configuration
server:
  port: 8081

app:
  environment: testing
  debug-enabled: true
  
  database:
    host: localhost
    port: 3306
    name: testdb
    username: testuser
    password: testpass
    
  api:
    endpoint: http://localhost:8082/api
    timeout: 3000
    retry-count: 2

logging:
  level:
    root: INFO
    com.masterclass: DEBUG
