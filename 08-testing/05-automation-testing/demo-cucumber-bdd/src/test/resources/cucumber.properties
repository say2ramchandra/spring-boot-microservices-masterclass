# Cucumber configuration

# Publishing
cucumber.publish.quiet=true

# Snippet type (camelcase or underscore)
cucumber.snippet-type=camelcase

# Default plugins (can be overridden via runner or command line)
# cucumber.plugin=pretty,html:target/cucumber-reports.html,json:target/cucumber-reports.json

# Default filter tags (run all if not specified)
# cucumber.filter.tags=@smoke
